export type MSFTMetadataFile = {
    cldr: string
    fromVersion: string
    glyph: string
    group: string
    keywords: string[]
    mappedToEmoticons: string[]
    tts: string
    unicode: string
    unicodeSkintones?: string[]
}
